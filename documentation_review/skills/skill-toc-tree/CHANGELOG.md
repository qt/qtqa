# skill-toc-tree Changelog

- **1.0.0** (2026-08-14): Initial version
  - Module tree via qhp subprojects (branch order, single-topic pattern)
  - The `<module>-toc.qdoc` page and `navigation.toctitles`
  - Framework tree merging via `tree_config.xml`
  - Procedures: add a page, create module docs, orphan audit
  - Warning catalogue and silent-failure list
  - Revised after 9-layer artifact test: Procedure C subtracts
    subproject indexTitle pages (no false ORPHANs); Module Contract
    corrected (landing page file, cppclassespage hedge); sortkey
    order under [descending] corrected; `<prefix>` placeholder
    clarified; trigger description widened; TOC-less module note
